<table>
  <tr>
    <th>Email</th>
    <th>Role</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.email, edit_admin_user_path(user) %></td>
      <td><%= user.roles %></td>
    </tr>
  <% end %>
</table>
